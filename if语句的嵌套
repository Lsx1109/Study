int a, b, c;
	scanf("%d %d %d", &a, &b, &c);
	int max = 0;
	if (a > b)
	    if (a > c)
		    max = a;
	    else
		    max = c;
	else
	    if (b > c)
		    max = b;
	    else
		    max = c;
	printf("max is %d\n", max);
  
  当if条件满足或者不满足的时候要执行的语句可以使if或if-else语句，这就是嵌套的if语句
  
  else总是和最近的那个if匹配
