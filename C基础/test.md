```mermaid
graph TD
    开始 --> 初始化π值
    初始化π值 --> 初始化计数器i
    初始化计数器i --> 输入半径
    输入半径 --> 读取半径
    读取半径 --> |半径小于0| 显示错误消息
    显示错误消息 --> 输入半径
    读取半径 --> 计算体积
    计算体积 --> 显示体积
    显示体积 --> 增加计数器i
    增加计数器i --> |i >= 5| 结束
    增加计数器i --> 输入半径
    结束 --> 结束
